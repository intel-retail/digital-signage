# Switch Simulation Video

1. Copy your `.avi` video to `pid/resources/videos/`.
2. Edit `pid/config.json` and update the `multifilesrc` file name in the pipeline.
3. Redeploy:

```bash
make down
make up
```
